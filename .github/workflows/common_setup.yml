# Here, I extracted the common setup steps which is
# the checkout code and setting up python
steps:
  - name: Checkout code
    uses: actions/checkout@v3

  - name: Set up Python
    uses: actions/setup-python@v4
    with:
      python-version: '3.10'
